# Admin Dashboard

React + Vite web app for admin management.

## Setup

```bash
npm install
cp .env.example .env   # set VITE_API_URL
npm run dev            # development
npm run build          # production build → dist/
```

See the root [README](../../README.md) for full details.
